<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="createtime" property="createtime" />
        <result column="role_id" property="roleId" />
        <result column="status" property="status" />
        <result column="affiliation" property="affiliation" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, username, password, createtime, role_id, status, affiliation
    </sql>

    <select id="pageC" resultType="com.example.entity.User">
        select * from user
    </select>

    <select id="pageCC" resultType="com.example.entity.User">
        select * from user ${ew.customSqlSegment}
    </select>
</mapper>
